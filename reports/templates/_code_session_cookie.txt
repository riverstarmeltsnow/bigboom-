===== LoginServlet.java =====

package com.web.test;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;
import java.io.IOException;
import java.net.URLEncoder;

@WebServlet("/login")
public class LoginServlet extends HttpServlet {
    private static final long serialVersionUID = 1L;

    @Override
    protected void doPost(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {
        request.setCharacterEncoding("utf-8");

        String username = request.getParameter("username");
        String password = request.getParameter("password");

        if ("hexin".equals(username) && "123456".equals(password)) {
            HttpSession session = request.getSession();
            session.setAttribute("username", username);
            response.sendRedirect("welcome.jsp");
        } else {
            String msg = URLEncoder.encode("用户名或密码错误", "UTF-8");
            response.sendRedirect("login.jsp?msg=" + msg);
        }
    }
}


===== LogoutServlet.java =====

package com.web.test;

import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;
import java.io.IOException;

@WebServlet("/logout")
public class LogoutServlet extends HttpServlet {
    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response)
            throws IOException {
        HttpSession session = request.getSession();
        session.invalidate();
        response.sendRedirect("login.jsp");
    }
}


===== login.jsp =====

<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>登录</title>
    <!-- CSS 样式略 -->
</head>
<body>
<div class="card">
    <h2>登 录</h2>
    <% String errMsg = request.getParameter("msg"); if (errMsg != null) { %>
        <div class="msg"><%= errMsg %></div>
    <% } %>
    <form action="${pageContext.request.contextPath}/login" method="post">
        <div class="form-group">
            <label for="username">用户名</label>
            <input type="text" id="username" name="username" placeholder="请输入用户名" required>
        </div>
        <div class="form-group">
            <label for="password">密码</label>
            <input type="password" id="password" name="password" placeholder="请输入密码" required>
        </div>
        <button type="submit" class="btn">登 录</button>
    </form>
</div>
</body>
</html>


===== welcome.jsp =====

<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ page import="java.util.Date" %>
<%@ page import="javax.servlet.http.Cookie" %>
<%@ page import="java.net.URLEncoder" %>
<%@ page import="java.net.URLDecoder" %>
<html>
<head><title>欢迎</title></head>
<body>
    <%
        String username = (String) session.getAttribute("username");
        if(username == null){
            response.sendRedirect(request.getContextPath() + "/login.jsp");
            return;
        }

        Cookie[] cookies = request.getCookies();
        String lastTime = null;
        if(cookies != null){
            for(Cookie c : cookies){
                if("lastVisit".equals(c.getName())){
                    lastTime = URLDecoder.decode(c.getValue(), "UTF-8");
                    break;
                }
            }
        }
        if(lastTime == null){
            out.print("这是您第一次登录<br>");
        } else {
            out.print("上次访问时间：" + lastTime + "<br>");
        }

        String nowStr = new java.text.SimpleDateFormat("yyyy-MM-dd HH:mm:ss").format(new Date());
        Cookie cookie = new Cookie("lastVisit", URLEncoder.encode(nowStr, "UTF-8"));
        cookie.setMaxAge(60 * 60 * 24 * 7);
        response.addCookie(cookie);
    %>
    <h2>欢迎：<%=username%></h2>
    <a href="${pageContext.request.contextPath}/profile.jsp">个人中心</a> |
    <a href="${pageContext.request.contextPath}/logout">退出登录</a>
</body>
</html>


===== profile.jsp =====

<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ page import="java.net.URLEncoder" %>
<html>
<head><title>个人资料</title></head>
<body>
    <%
        String username = (String) session.getAttribute("username");
        if(username == null) {
            response.sendRedirect("login.jsp?msg=" + URLEncoder.encode("请先登录", "UTF-8"));
            return;
        }
    %>
    <h2>个人资料页面</h2>
    <p>当前登录用户：<%= username %></p>
    <a href="${pageContext.request.contextPath}/welcome.jsp">返回欢迎页</a>
</body>
</html>
